#include <stdio.h>
int main()
{
    int age;
    printf("enter the age of voting");
    scanf("%d", &age);

    if (age < 18)
    {
        printf("you are not eligible for voting");
    }
    else if (age >= 18)
    {
        printf("you are eligible for voting");
    }
    return 0;
}
