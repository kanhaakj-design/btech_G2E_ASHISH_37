#include <stdio.h>
int main()
{
    int a, b, c;
    printf("enter sthe sides of triangle");
    scanf("%d %d %d", &a, &b, &c);
    if (a == b && b == c)
    {
        printf("the triangle is equitareal");
    }
    else if (a == b || b == c || c == a)
    {
        printf("the triangle is isocelese");
    }
    else
    {
        printf(" the triangle id scalene ");
    }

    return 0;
}
