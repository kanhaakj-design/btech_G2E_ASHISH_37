#include <stdio.h>
int main()
{
    int num;

    printf("enter the number t0 be checked");
    scanf("%d", &num);

    if (num % 2 == 0)
    {
        printf("the number entered is even");
    }
    else if (num % 1 == 0)
    {
        printf("the number entered is odd");
    }

    return 0;
}
