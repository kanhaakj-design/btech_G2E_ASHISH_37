#include <stdio.h>
int main()
{

    int percentage;
    char *grade;

    printf("enter the grade according to percentage");
    scanf("%d", &percentage);

    if (percentage > 90 && percentage <= 100)
    {
        printf(" grade  A");
    }
    else if (percentage > 80 && percentage <= 90)
    {
        printf("grade B");
    }
    else if (percentage > 60 && percentage <= 80)
    {
        printf("grade C");
    }
    else if (percentage <= 60)
    {
        printf("grade D");
    }
    return 0;
}
